name: Test fails without credentials
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Create publish directory
        run: mkdir -p /tmp/publish-dir && echo "Hello World" > /tmp/publish-dir/index.html

      - id: action
        uses: .
        continue-on-error: true
        with:
          publish-dir: /tmp/publish-dir
          fails-without-credentials: "true"
        # No NETLIFY_AUTH_TOKEN or NETLIFY_SITE_ID set

      - name: Assert action failed as expected
        env:
          OUTCOME: ${{ steps.action.outcome }}
        run: |
          echo "outcome: $OUTCOME"
          if [ "$OUTCOME" != "failure" ]; then
            echo "FAIL: expected action to fail when fails-without-credentials=true and no credentials provided, but got outcome: $OUTCOME"
            exit 1
          fi
          echo "PASS: action correctly failed without credentials"
